import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:microblogging/app/modules/posts/posts_controller.dart';
import 'package:microblogging/app/modules/posts/posts_module.dart';

void main() {
  initModule(PostsModule());
  // PostsController posts;
  //
  setUp(() {
    //     posts = PostsModule.to.get<PostsController>();
  });

  group('PostsController Test', () {
    //   test("First Test", () {
    //     expect(posts, isInstanceOf<PostsController>());
    //   });

    //   test("Set Value", () {
    //     expect(posts.value, equals(0));
    //     posts.increment();
    //     expect(posts.value, equals(1));
    //   });
  });
}
