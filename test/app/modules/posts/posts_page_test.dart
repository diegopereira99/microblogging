import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:microblogging/app/modules/posts/posts_page.dart';

void main() {
  testWidgets('PostsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PostsPage(title: 'Posts')));
    //  final titleFinder = find.text('Posts');
    //  expect(titleFinder, findsOneWidget);
  });
}
